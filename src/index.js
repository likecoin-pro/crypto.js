import {hash, xhash, privateKeyBySecret, publicKeyByPrivate, addressByPublic} from "./lib"

const crypto = {
    hash,
    xhash,
    privateKeyBySecret,
    publicKeyByPrivate,
    addressByPublic
};

export default crypto;